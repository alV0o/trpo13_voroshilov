<Page x:Class="trpo12_voroshilov.Pages.UsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:trpo12_voroshilov.Pages"
      xmlns:m="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:validators="clr-namespace:trpo12_voroshilov.Validators"
      xmlns:services ="clr-namespace:trpo12_voroshilov.Service"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UsersPage">
    <Page.Resources>
        <services:RolesService x:Key="roles"/>
    </Page.Resources>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <StackPanel>
                <Label Content="Логин" HorizontalAlignment="Center"/>
                <TextBox x:Name="loginTextBox" m:HintAssist.Hint="Введите логин">
                    <Binding Path="Login" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:ValidationLogin/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Label Content="Имя пользователя" HorizontalAlignment="Center"/>
                <TextBox Text="{Binding Username}"
                         m:HintAssist.Hint="Введите имя пользователя"/>
            </StackPanel>
            <StackPanel>
                <Label Content="Электронная почта" HorizontalAlignment="Center"/>
                <TextBox x:Name="emailTextBox" m:HintAssist.Hint="Введите электронную почту">
                    <Binding Path="Email" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules >
                            <validators:ValidationEmail/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Label Content="Пароль" HorizontalAlignment="Center"/>
                <TextBox x:Name="passwordTextBox" m:HintAssist.Hint="Введите пароль">
                    <Binding Path="Password" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <validators:ValidationPassword/>
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox>
            </StackPanel>
            <ComboBox  m:HintAssist.Hint="Роли" ItemsSource="{Binding Source={StaticResource roles}, Path=Roles}" DisplayMemberPath="Title" SelectedItem="{Binding Role}"/>
            <Button Content="Создать" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Page>
